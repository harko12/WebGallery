using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace WebGallery.UIBL
{
    public enum DateGroups { Year = 1, Month = 2, Date = 3 };

    public static class K
    {
        public const string SG_CURRENT_GALLERY = "sg_current_gallery";
    }
}
